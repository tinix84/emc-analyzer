{
  "buildCommand": "npm run build:wasm && npx nuxt build --no-prepare",
  "outputDirectory": ".output/public",
  "installCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source ~/.cargo/env && rustup target add wasm32-unknown-unknown && cargo install wasm-pack && npm install --legacy-peer-deps",
  "framework": "nuxtjs"
}