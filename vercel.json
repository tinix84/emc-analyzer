{
  "buildCommand": "chmod +x build.sh && ./build.sh",
  "outputDirectory": ".output/public",
  "installCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source ~/.cargo/env && cargo install wasm-pack && npm install",
  "framework": "nuxtjs"
}